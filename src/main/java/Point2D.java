public interface Point2D { //maybe we're never gonna need this, like, will we ever want to build a 3d Logo thing?

    int getX();
    int getY();
    void setX (int value);
    void setY (int value);

}
